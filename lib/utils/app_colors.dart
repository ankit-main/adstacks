import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Background
  static const Color background = Color(0xFFF5F5F7);

  // Sidebar
  static const Color sidebarBg = Color(0xFFFFFFFF);

  // Dark Navy Cards
  static const Color navy = Color(0xFF0F1F5C);
  static const Color navyLight = Color(0xFF16296D);

  // Purple
  static const Color primary = Color(0xFF7B61FF);
  static const Color primaryLight = Color(0xFFB66DFF);

  // Banner Gradient
  static const Color bannerStart = Color(0xFF6C2DFF);
  static const Color bannerMiddle = Color(0xFFB25BFF);
  static const Color bannerEnd = Color(0xFFFFB6C8);

  // Project Card
  static const Color projectRed = Color(0xFFE84F75);

  // Text
  static const Color textPrimary = Color(0xFF1F1F1F);
  static const Color textSecondary = Color(0xFF757575);

  // White
  static const Color white = Colors.white;

  // Border
  static const Color border = Color(0xFFE7E7E7);

  // Chart
  static const Color chartBlue = Color(0xFF5246FF);
  static const Color chartPink = Color(0xFFFF8FAE);

  // Calendar
  static const Color selectedDay = Color(0xFF6E4DFF);

  // Birthday Button
  static const Color buttonPurple = Color(0xFFA35BFF);
}